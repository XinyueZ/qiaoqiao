<?xml version="1.0" encoding="utf-8"?>
<resources>
    <array name="glowpadview_target_drawables">
        <item>@drawable/ic_from_web_button</item>
        <item>@drawable/ic_capture_button</item>
        <item>@drawable/ic_from_local_button</item>
        <item>@drawable/ic_from_videocam_button</item>
    </array>
    <item name="glowpadview_target_handle_drawable" type="drawable">@drawable/ic_touch_app_button</item>
    <!-- Default target placement radius for GlowPadView. Should be 1/2 of outerring diameter. -->
    <dimen name="glowpadview_target_placement_radius">100dip</dimen>
    <!-- Default glow radius for GlowPadView -->
    <dimen name="glowpadview_glow_radius">75dip</dimen>
    <!-- Default distance beyond which GlowPadView snaps to the matching target -->
    <dimen name="glowpadview_snap_margin">40dip</dimen>
    <!-- Default distance from each snap target that GlowPadView considers a "hit" -->
    <dimen name="glowpadview_inner_radius">15dip</dimen>
    <!-- Size of lockscreen outerring on unsecure unlock LockScreen -->
    <dimen name="keyguard_lockscreen_outerring_diameter">200dp</dimen>
    <!-- Circle size for incoming call widget's each item. -->
    <dimen name="incoming_call_widget_circle_size">56dp</dimen>
    <!-- Margin used for incoming call widget's icon for each item.
         This should be same as "(incoming_call_widget_circle_size - icon_size)/2".
         Right now answer/decline/reject icons have 38dp width/height.
         So, (94 - 38)/2 ==> 28dp -->
    <dimen name="incoming_call_widget_asset_margin">9dp</dimen>

</resources>