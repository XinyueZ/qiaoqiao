<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data class="LogoViewBinding">

        <variable
            name="visionEntity"
            type="com.qiaoqiao.core.camera.vision.model.VisionEntity"/>

        <variable
            name="viewholder"
            type="com.qiaoqiao.core.camera.vision.ui.viewholder.LogoViewHolder"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/vision_iv"
            style="@style/VisionImageView"
            android:onClick="@{(v) -> viewholder.onClicked(visionEntity)}"
            android:layout_weight="1"
            android:baselineAligned="false"/>

        <FrameLayout
            android:id="@+id/vision_tv_fl"
            style="@style/VisionItemRoot"
            android:background="@drawable/selector_round_blue_gray_rectangle_item_background"
            android:onClick="@{(v) -> viewholder.onClicked(visionEntity)}">


            <TextView
                android:id="@+id/vision_tv"
                style="@style/VisionItemTextView"
                android:drawableEnd="@drawable/ic_knowledge_link_open"
                android:drawableRight="@drawable/ic_knowledge_link_open"
                tools:text="hello,world"/>

        </FrameLayout>
    </LinearLayout>
</layout>