<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data class="GroupBinding">


        <variable
            name="title"
            type="String"/>

        <variable
            name="description"
            type="String"/>


    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/larger"
        android:paddingLeft="@dimen/larger"
        android:paddingRight="@dimen/larger"
        android:paddingTop="@dimen/larger"
        >

        <TextView
            android:id="@+id/licence_title_tv"
            style="@style/TextAppearance.AppCompat.Medium"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="left"
            android:text="@{title}"
            android:textStyle="bold"
            />

        <TextView
            android:id="@+id/licence_description_tv"
            style="@style/TextAppearance.AppCompat.Small"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="left"
            android:text="@{description}"
            />
    </LinearLayout>
</layout>